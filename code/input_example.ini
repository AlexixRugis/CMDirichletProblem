# ============================================================
#  Входной файл для консольного решателя задачи Дирихле
#  Комментарии начинаются с #
# ============================================================

# Выбор задачи:
#   0 — тестовая (сравнение с точным решением u)
#   1 — основная (контроль через сетку с половинным шагом)
problem = 0

# Область: [a,b] x [c,d]
a = -1.0
b =  1.0
c = -1.0
d =  1.0

# Число разбиений по x и y (основная сетка)
n = 200
m = 200

# Максимальное число итераций
nmax = 100000000

# Критерий остановки итерационного метода (основная сетка)
epsn  = 1e-9

# Критерий остановки для сетки половинного шага (только для problem=1)
eps2n = 1e-11

# Метод решения СЛАУ:
#   0 — метод Зейделя
#   1 — МВР / SOR
method = 0

# Релаксационные параметры (используются только при method=1)
# w  — для основной сетки,  должно быть в (0, 2)
# w2 — для сетки половинного шага, должно быть в (0, 2)
w  = 1.8
w2 = 1.9
